import { StyleSheet } from "react-native";

export const TitelStyle = StyleSheet.create({
    TextStyle: {
        fontSize: 40,
        fontWeight: "bold",
        textAlign: "center",
        color: "black",
        marginTop:10
    
        },
});
