import { StyleSheet } from "react-native";

export const BlockStyle = StyleSheet.create({
    container: {
   
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom:50,
        marginTop:30,
      },
      row: {
        flexDirection: 'row',
      },

});
